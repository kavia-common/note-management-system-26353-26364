# Frontend environment variables for notes_frontend

# Base URL of the FastAPI backend (e.g., http://localhost:8000 or the deployed URL)
REACT_APP_API_BASE_URL=http://localhost:8000

# Storage key for auth token (change if needed)
REACT_APP_AUTH_TOKEN_KEY=notes_auth_token
